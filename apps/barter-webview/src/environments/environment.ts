export const environment = {
  production: false,
  apiUrl: '/api',
  telegramBotToken: 'YOUR_BOT_TOKEN', // Replace with your bot token
  telegramBotUsername: 'YOUR_BOT_USERNAME', // Replace with your bot username
  // For local development, we'll use ngrok to expose our local server
  ngrokUrl: 'YOUR_NGROK_URL', // Will be updated when running ngrok
};
